version: '3.8'

services:
    backend:
        restart: always
        command: sleep 60; gunicorn backend.wsgi --bind 0.0.0.0:8000
        build:
            context: ./backend
            dockerfile: Dockerfile
        expose:
            - "8000"

    nginx:
        build: ./nginx
        ports:
            - "8008:80"
        depends_on:
            - backend

