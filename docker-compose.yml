version: '3.8'

services:
    backend:
        restart: always
        build:
            context: ./backend
            dockerfile: Dockerfile
        ports:
            - 8000:8000

    nginx:
        build: ./nginx
        ports:
            - "8000:80"
        depends_on:
            - backend

